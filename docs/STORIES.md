# SkillOS — User Stories & Build Tracker (v0.3)

Rewritten after the 2026-09-12 pivot (DECISIONS.md §12). Earlier v0.2 stories S-01..S-07 were completed and then superseded; their reusable parts (UI system, Persian infra, page tests) carry over.

Status: `[ ]` todo · `[~]` in progress · `[x]` done · `[-]` dropped

---

## M0 — Catalog foundation

### [x] S-01 Schema v0.3
Tables per DESIGN.md §3: courses, lessons, lesson_videos, lesson_prerequisites, activities(lesson_id, key), enrollments, attempts, mastery_records(lesson_id), plan_items(enrollment_id). Old learning_items/skills/skill_dependencies/resources removed. `migrate:fresh` clean; models, factories, relationships in place.

### [x] S-02 Content importer
As the owner, I want `php artisan content:import <slug>` to load a course from `content/<slug>/` so authoring stays in files.
- Upserts course by slug, lessons by slug, learn activity per lesson, practices by key; videos and prerequisites replaced from the file; default prerequisite = previous lesson.
- Re-import after editing a lesson text keeps existing attempts/mastery (same ids).
- Validates: unique slugs, prerequisite slugs exist, mcq has 4 options and a valid correct_option, hints ≤ 2, difficulty/form enums. Errors name the file and field.
- A sample course in `content/sample-course/` (2–3 short lessons) ships with the repo for tests and demos.

### [x] S-03 Catalog and enrollment
As a learner, I want to see all courses and enroll.
- `/courses` lists courses with lessons count and an "enrolled" badge; `/courses/{slug}` shows outcome, sources, lessons table (level per lesson for me), enroll button.
- Enroll creates an enrollment (active, priority 3, no daily time) and redirects to the course page; enrolling twice is a no-op.

### [x] S-04 Enrollment config
Priority, Daily Time, Preferred time, Status — same form and rules as before (v0.2 S-04/edit page), now on `/enrollments/{id}/edit`.

### [x] S-05 Lesson page
As a learner, I want to open a lesson and switch between video and text.
- Tabs ویدیو / متن (video tab first and marked Recommended when a video exists); markdown text; key points and common mistakes; practices list with form/difficulty/minutes; prerequisites with my levels; my level badge.
- Locked state shown (with reason) when a prerequisite is below Familiar; the lesson can still be opened (free exploration).

### [x] S-06 My courses (interim Home)
`/` for a logged-in user lists enrolled courses with a level-distribution bar and "unscheduled" flag, plus a link to the catalog. Replaced by the real Home in M4.

## M1 — First real course

### [x] C-01 Advanced Python course content (authoring done 2026-09-14; owner review ongoing)
Owner provided the videos + subtitles + exercise files as `course/python-deep-dive-1/`, served via `public/media/` symlink. `content/python-deep-dive-1/` has all 106 lessons (159 videos, 11 sections), full text + practices for «مرور سریع» (10), «متغیرها و حافظه» (11), «انواع عددی» (18), «پارامترهای تابع» (10), «توابع درجه‌یک» (10) «دامنه‌ها، closureها و decoratorها» (12), «tuple و named tuple» (6), «ماژول‌ها، پکیج‌ها و namespaceها» (12), «به‌روزرسانی‌های پایتون» (8) and «مطالب تکمیلی» (9) — **all 106 lessons** authored (258 practices) from English transcripts + slides + notebooks + example files. 9 English subtitles are empty upstream (055, 056, 064, 078, 085, 097, 106, 111, 138; parked in `course/.../broken-subs/`). Claude writes (lesson texts following the videos, 2–3 practices per lesson with rubrics), imports, owner reviews in the app, fixes iterate as commits.
2026-09-16: swept the 41 lessons still carrying leftover Persian-in-code-fence comments (187 lines, translated to English, code untouched) — the English-comments house rule postdated this course's authoring, so it had never been retroactively applied here. `validate.py python-deep-dive-1` now reports 0 warnings, matching `complete-python-mastery` (DECISIONS.md §47).

### [x] C-02 «پایتون از صفر» beginner course content (all 133 lessons across 11 sections authored 2026-09-16)
Owner provided `course/complete-python-mastery/` (Mosh's "Complete Python Course", 181 lectures) — only lectures **001–139** are in scope (fundamentals → OOP → stdlib → packaging); 140–184 (APIs/automation mini-project, Django project, ML intro) were explicitly dropped and their video/subtitle files deleted. Source filenames arrived as download-tool artifacts (`index.html?token=...&filename=...`) — renamed to the `NNN-kebab-title.{mp4,en.srt,fa.srt}` convention (script kept in `tools/authoring/` history, not the repo), then `srt2vtt.py`. `content/complete-python-mastery/course.json` has all **133 lessons** (136 lectures, 3 merged: VSCode-tricks win/mac, functions exercise+solution, managing-dependencies pt1/pt2) across 11 sections — «شروع به کار» (12), «متغیرها و انواع داده» (10), «عبارت‌های شرطی» (7), «حلقه‌ها» (7), «توابع» (11), «ساختارهای داده‌ی درونی» (23), «استثناها» (7), «برنامه‌نویسی شی‌گرا» (22), «ماژول‌ها و پکیج‌ها» (8), «کتابخانه‌ی استاندارد پایتون» (17), «مدیریت پکیج‌ها» (9) — titles/summaries/videos only, one-paragraph placeholder `.md` per lesson, **no practices/key_points yet**. «شروع به کار» (12 lessons, 19 practices) fully authored 2026-09-14 — first lesson renamed to the `course-overview` slug (matches the validator's no-practices exemption used by C-01). «متغیرها و انواع داده» (10 lessons, 21 practices) authored 2026-09-15; also the point where the owner asked for a simpler/casual tone across all lesson text (not just this course) — both sections rewritten to match, see `feedback_lesson_tone` in Claude's memory and `docs/AUTHORING.md` §5. All 40 practices across both sections then rewritten again the same day around real, tangible scenarios instead of "what does this print" drills — section 1 as help-a-friend/code-review moments, section 2 around one running "user profile" scenario (avatar initials, masking a national ID, a signup form, a shopping cart) — per `docs/AUTHORING.md` §6's updated practice-design note. No emoji anywhere in learner-facing text (explicit owner preference). «عبارت‌های شرطی» (7 lessons, 19 practices) authored 2026-09-15 — threaded the video's own loan/university-eligibility examples as the running scenario (no need to invent a parallel one); one real bug caught and fixed while drafting the logical-operators lesson (a wrong claim that dropping the parentheses around `high_income or good_credit` wouldn't change the result — it does, `and` binds tighter than `or`), then turned into its own "stretch" practice testing operator precedence. «حلقه‌ها» (7 lessons, 16 practices) authored 2026-09-15 — threaded the video's own retry/send-message scenario across for-loops → for-else → infinite-loops (three different ways to express the same "retry up to N times" idea), and the video's own echo/quit REPL loop across while-loops → infinite-loops; exercise-loops' practices extend the video's own even-number exercise (count odds, then a bugged sum-vs-count explanation practice) rather than repeating it verbatim. «توابع» (11 lessons, 24 practices) authored 2026-09-15 — includes two merged-video lessons already set up in the skeleton (`vscode-coding-tricks`: Windows+Mac shortcut videos merged into one lesson with a comparison table; `exercise-functions`: the FizzBuzz exercise + solution videos merged into one worked walkthrough). The FizzBuzz solution's real "more specific condition must come first" bug (checking `% 3` before the combined `% 3 and % 5` check swallows FizzBuzz cases) became the section's throughline practice, reused in a second scenario (age classification) so it's tested as a transferable principle, not just recalled from the exact video example. «ساختارهای داده‌ی درونی» (23 lessons, the biggest section — split into two authoring passes per `docs/AUTHORING.md` §4's 6–12-lesson guidance): first 12 (lists, accessing-items, list-unpacking, looping-over-lists, adding-or-removing-items, finding-items, sorting-lists, lambda-functions, map-function, filter-function, list-comprehensions, zip-function — 28 practices) authored 2026-09-15, threading the video's own "order items" `(product, price)` tuple list as one running scenario across sorting→lambda→map→filter→comprehensions (the same problem solved five different ways, back to back). Also fixed a latent issue found while writing this batch: several `key_points`/`common_mistakes` strings across earlier sections (`conditional-statements`, `ternary-operator`, `while-loops`, `defining-functions`, `keyword-arguments`, `default-arguments`, `args`, `kwargs`, `sorting-lists`, `lambda-functions`, `map-function`, `filter-function`) had Persian placeholder words inside backtick code-spans (e.g. `` `if شرط:` ``) — `validate.py` only checks fenced code blocks in `.md` files, not inline backticks inside `course.json` strings, so it slipped through; all 13 instances now read in English (`` `if condition:` ``). Remaining 11 (stacks, queues, tuples, swapping-variables, arrays, sets, dictionaries, dictionary-comprehensions, generator-expressions, unpacking-operator, exercise-collections — 30 practices) authored 2026-09-15, completing the section (**«ساختارهای داده‌ی درونی» all 23 lessons, 58 practices total**). One real transcript error caught and corrected: the source auto-captions said `queue.pop()` removes from the front of a `deque`, but `deque.pop()` actually removes from the right end same as a list — `popleft()` is the one that's O(1) from the front (the entire point of using `deque` over a list for a queue), so the lesson and practices use `popleft()`, not the transcript's literal wording. `exercise-collections`' worked example (most-repeated-character) uses a self-verified `"hello world"` sentence (not the source video's, which was ambiguous in the auto-captions) so the stated character counts are provably correct rather than guessed. «استثناها» (7 lessons, 18 practices) authored 2026-09-15 — try/except/else, multiple except clauses (separate vs shared tuple), finally for cleanup, the with statement (context management protocol), raise, and measuring the real performance cost of raising vs a plain if (via timeit). Threaded one running scenario (calculate_x_factor(age) = 10/age) across handling-exceptions → handling-different-exceptions → raising-exceptions → cost-of-raising-exceptions. Caught and fixed an inaccuracy while drafting cleaning-up: the transcript implied moving `file.close()` after the try/except block simply "doesn't run" on failure — actually it does run, and raises a fresh `NameError` since `file` was never assigned; corrected to the accurate failure mode. «برنامه‌نویسی شی‌گرا» (22 lessons, second-biggest section — split into two authoring passes): first 11 (classes, creating-classes, constructors, class-vs-instance-attributes, class-vs-instance-methods, magic-methods, comparing-objects, performing-arithmetic-operations, making-custom-containers, private-members, properties — 30 practices) authored 2026-09-15, threading one running `Point`-style scenario through the early lessons and a `TagCloud` custom-container class continuously across making-custom-containers → private-members (built up incrementally: dict-backed storage → case-insensitive tags → `__getitem__`/`__setitem__`/`__len__`/`__iter__` → double-underscore privacy and name-mangling). `properties` closes the batch with the `Product`/price-validation progression from explicit `get_price`/`set_price` methods to the built-in `property()` function to the idiomatic `@property`/`@price.setter` decorator pair, plus the read-only-property gotcha (`AttributeError` when no setter is defined). Six more Persian-in-code-fence instances caught and fixed during this batch's scan (making-custom-containers.md inline comments, properties.md inline comments) — same latent gap as before (validate.py doesn't check inline comments/backtick spans). Remaining 11 (inheritance, the-object-class, method-overriding, multi-level-inheritance, multiple-inheritance, a-good-example-of-inheritance, abstract-base-classes, polymorphism, duck-typing, extending-built-in-types, data-classes — 33 practices) authored 2026-09-15, completing the section (**«برنامه‌نویسی شی‌گرا» all 22 lessons, 63 practices total**). Threads one Animal/Mammal/Fish scenario through inheritance → the-object-class → method-overriding → multi-level-inheritance (bird/chicken abuse example) → multiple-inheritance (Employee/Person MRO ambiguity, then a clean Flyer/Swimmer/FlyingFish counterexample), then a second running Stream/FileStream/NetworkStream scenario through a-good-example-of-inheritance → abstract-base-classes (adding ABC + `@abstractmethod` to fix the two design gaps: direct instantiation and unenforced `read` contract), then a third UIControl/TextBox/DropDownList scenario through polymorphism → duck-typing (removing the ABC entirely to show polymorphism still works via duck typing, then explaining why keeping the ABC is still good practice). extending-built-in-types covers subclassing `str`/`list` directly (Text.duplicate, TrackableList.append via `super()`). Caught one real content/course.json mismatch while prepping data-classes: the skeleton's placeholder title/summary said "دکوراتور dataclass" but lecture 104's actual transcript teaches `collections.namedtuple` as the lightweight alternative to a hand-written `__eq__`, not the `@dataclass` decorator — retitled the lesson to «تاپل‌های نام‌دار (Named Tuple)» and rewrote the summary to match the real content rather than the guessed placeholder. Ten more Persian-in-code-fence instances (inline comments) caught and fixed during this batch's scan across inheritance.md, the-object-class.md, abstract-base-classes.md, extending-built-in-types.md, data-classes.md. «ماژول‌ها و پکیج‌ها» (8 lessons, 24 practices) authored 2026-09-15 — threads one running `ecommerce`/`sales` scenario across the whole section, exactly following the video's own progression: creating-modules (splitting app.py into sales.py, three import styles, why `import *` is discouraged) → compiled-python-files (`__pycache__`, bytecode caching, why the entry-point script itself is never cached) → module-search-path (`sys.path`, search order) → packages (moving sales.py into an `ecommerce/` folder, `__init__.py`, three ways to import from a package) → sub-packages (`ecommerce/shopping/`, nested `__init__.py`) → intra-package-references (a sibling `customer/contact.py` package, absolute vs. relative imports, PEP8's absolute-import preference) → the-dir-function (`dir()`, `__name__`/`__package__`/`__file__`) → executing-modules-as-scripts (module-level code runs once on first import, `__name__ == "__main__"` idiom for dual script/library usage). Five more Persian-in-code-fence instances (inline comments) caught and fixed during this batch's scan across intra-package-references.md and the-dir-function.md. «کتابخانه‌ی استاندارد پایتون» (17 lessons, split into two authoring passes): first 11 (python-standard-library, working-with-paths, working-with-directories, working-with-files, working-with-zip-files, working-with-csv-files, working-with-json-files, working-with-a-sqlite-database, working-with-timestamps, working-with-datetimes, working-with-time-deltas — 32 practices) authored 2026-09-15. Follows the video's own progression through `pathlib.Path` (construction, inspecting, `with_name`/`with_suffix` not touching disk) → directories (`iterdir` as a generator, `glob`/`rglob`) → files (`read_text`/`write_text` vs. raw `open()`, `stat()`, `shutil.copy`) → zip/csv/json (each threading a small "ecommerce"/"movies" example) → SQLite (connect → CREATE TABLE IF NOT EXISTS → placeholder-based INSERT → commit → cursor iteration, explicitly note the video's DB-Browser-GUI table creation was replaced with a `CREATE TABLE IF NOT EXISTS` SQL statement so the lesson stays fully code-based and reproducible) → time/datetime/timedelta (epoch timestamps, importing the `datetime` class directly instead of `datetime.datetime`, `strptime`/`strftime` as inverses, why `timedelta` has no month/year units). 37 Persian-in-code-fence instances (inline comments, the largest batch of this recurring issue yet) caught and fixed during this batch's scan. Remaining 6 (generating-random-values, opening-the-browser, sending-emails, templates, command-line-arguments, running-external-programs — 17 practices) authored 2026-09-16, completing the section (**«کتابخانه‌ی استاندارد پایتون» all 17 lessons, 49 practices total**). Threads one running email-sending scenario across sending-emails → templates (build a `MIMEMultipart` message by hand, then move its body into a separate `template.html` file filled via `string.Template`). Real credentials from the source video (a throwaway test Gmail account and its password, since deleted by the course author) were replaced with generic placeholders plus an explicit security note about never hardcoding real credentials — not a transcript error to preserve faithfully, a case where following the source literally would have been actively bad practice to teach. «مدیریت پکیج‌ها» (9 lessons, 22 practices) authored 2026-09-16 — the last section of the course. Follows the video's own progression: pypi (what a third-party package index is) → pip (install/list/uninstall, semantic versioning, `==`/`~=`/`.* ` version pins) → virtual-environments (`venv`, the "only one version of a package at a time" problem) → pipenv (combines pip + venv into one tool) → virtual-environments-in-vscode (pointing the editor at the venv's interpreter) → pipfile (`Pipfile` vs. `Pipfile.lock`, `--ignore-pipfile` for reproducible installs) → managing-dependencies (two merged videos: `pipenv graph`/`uninstall`/`update --outdated`, then publishing a package to PyPI end-to-end with `setup.py`/`sdist`/`bdist_wheel`/`twine`) → docstrings → pydoc, closing the whole course. Retitled managing-dependencies' summary (skeleton said "ثبت و نصب دقیق وابستگی‌ها... requirements.txt") to also cover the second video's actual content (publishing packages) — same kind of placeholder-summary mismatch as data-classes earlier. Replaced the source video's real author name/package example with a generic "mypdf" placeholder throughout the publishing walkthrough. Ten more Persian-in-code-fence instances (inline comments inside `bash` fences, not just `python` ones) caught and fixed during this batch's scan. **«پایتون از صفر» is now fully authored end to end**: `python3 tools/authoring/validate.py complete-python-mastery` reports 0 problems across all 133 lessons, and `php artisan content:import` loads 133 lessons / 335 practices cleanly.

## M2 — Session (unchanged from v0.2 S-08..S-13, "skill" → "lesson")

### [x] S-08 Learn activity (tabs video/text, "آماده‌ام" → completed)
### [x] S-09 Practice rendering by form
### [x] S-10 Rule evaluation (mcq)
### [x] S-11 AI evaluation (`evaluate_response`, Persian feedback, no score)
### [x] S-12 Hint → Retry → Answer loop, give up, evidence json
### [x] S-13 Feedback + Next (interim: next practice of the same lesson / retry / back to lesson — planner-driven Next arrives in M4)

## M3 — Mastery

### [x] S-14 MasteryService (per lesson; DECISIONS §3; unit-tested table)
### [x] S-15 Level display on lesson/course pages; course progress summary; no numeric leaks

## M4 — Planning, Continue Learning, Home

### [x] S-16 `Planner::today` (DECISIONS §2, per enrollment; fixtures tested)
### [x] S-17 Continue Learning (recommendation + 2 alternatives; plan item completion rules)
### [x] S-18 Home (CTA, Today per course, My courses)
### [x] S-19 Recompute today's plan on enrollment config change

## M5 — Week, Skip, Lifecycle

### [x] S-20 Week view · [x] S-21 Skip · [x] S-22 Pause/Archive/Maintenance · [x] S-23 Reactivation after >14 days

## M6 — Real use

### [ ] S-24 One week of daily use on the Python course; tune DECISIONS §3 numbers with dated notes.

## M7 — Engagement (habit-formation, cross-course)

### [x] S-25 Streak, daily-goal progress bar, short-gap nudge on Home (DECISIONS §21)
### [x] S-26 Weekly recap widget · [x] S-27 Cross-course monthly stats (shipped together as one card, DECISIONS §22)
### [x] S-28 Level-up celebration polish (only past "learning", DECISIONS §23)
### [x] S-29 Review-retention stat on the lesson page (DECISIONS §24)
### [x] S-30 "5-minute mode" quick-review button on Home (DECISIONS §25)
### [x] S-31 Global search across lessons (DECISIONS §26)
### [x] S-32 Friends streak/stats view (DECISIONS §27) — engagement batch (M7) complete

## M8 — Learning-science pieces

### [x] S-33 Weak spots page (recurring wrong lessons, cross-course)
### [x] S-34 Seen-vs-mastered mark on the course lesson table
(Both DECISIONS §28. Interleaving, confidence calibration, a concept map, and adaptive difficulty were considered and explicitly rejected for this app's scale — see §28 for why; pretesting wasn't asked for.)

## M9 — Review system at scale (multiple courses)

### [x] S-35 Group Home's «امروز» list by course, section headers instead of a per-row tag (DECISIONS §29)
Today's list is currently flat (every course's items interleaved, ordered by `Planner::orderForLearner`), with the only per-item course indicator a small gray tag that's `hidden` below the `sm` breakpoint — invisible on mobile. Owner confirmed the review *algorithm* itself (intervals, AI-fresh review content §18, retention stat §24) is solid and needs no change; this story is specifically about the list becoming hard to scan as more courses are added, especially on a phone. Plan: group items under a per-course section heading (course title always visible, mobile included) instead of the row-level tag; keep each course's internal ordering as-is (reviews first); leave the "ادامه‌ی یادگیری" primary CTA untouched — this only reshapes the full-list card below it. A global (cross-course) daily review cap was considered and explicitly deferred — not rejected — since `week()` already gives forward visibility into review load; revisit only if pileups turn out to be a real problem in practice.

### [ ] C-03 «مسیر رهبری فنی» — 10 courses from `course/Management course/` (2026-09-16, in progress)
Owner dropped `course/Management course/` — 10 numbered sub-folders (559 lectures total, 36G), each a separate Udemy-style course by a different instructor, spanning requirements engineering, software architecture (×3), product management, agile/scrum, engineering management, tech/org leadership, and AI-era leadership. Discussed with the owner before touching anything: **10 separate `Course` rows**, not one giant course (per-course enrollment/pacing/streak/planner already assumes one coherent subject; these 10 are unrelated topics that should be startable independently — zero schema change either way). The umbrella name **«مسیر رهبری فنی»** is a *conceptual* grouping for how these are introduced, not a DB entity — no track/program layer exists and none was requested. Same full authoring depth (practices+rubrics, key_points, common_mistakes, scenario-threading) as C-01/C-02 for all 10, owner's explicit choice over a lighter format. A separate `course/scrum course/` folder held 2 more courses, originally set aside as out of scope for C-03 — now picked up as its own story, C-04 below.

Planned course list, in build order (folder # → slug):
1. `requirements-engineering` — Requirements Engineering (IREB/INCOSE), 86 lectures — **in progress, this is first**
2. `software-architecture-complete-guide` — Complete Guide to Becoming a Software Architect, 113 lectures
3. `system-design-case-studies` — Software Architecture & System Design: Practical Case Studies, 19 lectures
4. `large-scale-systems-design` — Architecture & Design of Modern Large-Scale Systems, 41 lectures
5. `becoming-a-product-manager` — Becoming a Product Manager, 109 lectures
6. `advanced-scrum-master` — Ultimate Advanced Training for Agile Scrum Master, 59 lectures
7. `practical-guide-for-tech-leaders` — Practical Guide for Leaders in Tech, 34 lectures
8. `become-an-engineering-manager` — Become an Engineering Manager, 66 lectures
9. `organizational-leadership-ai-era` — Organizational Leadership in the Era of AI, 15 lectures
10. `generative-ai-in-organizations` — Introducing Generative AI into Your Organization, 17 lectures

**➡ Recommended learning order (2026-09-17, owner-facing, documentation only — no in-app track/badge, per §57):** the build order above *is* the recommended order — it wasn't arbitrary, it groups into three stages: **(1) technical foundations** — requirements engineering → software architecture → system design case studies → large-scale systems design, each building on the previous one's vocabulary and depth; **(2) product & process** — becoming a product manager → advanced scrum master, the disciplines a technical lead works alongside daily; **(3) leadership progression** — practical guide for tech leaders → become an engineering manager → organizational leadership in the AI era → generative AI in organizations, ending on the two most forward-looking, least foundational topics. Someone new to the whole bundle should go top to bottom; someone who already has solid engineering/architecture background could reasonably start at course 5. Full rationale in `docs/DECISIONS.md` §57.

### [~] C-04 Two more Scrum courses from `course/scrum course/` (2026-09-17, `scrum-master-ai-bootcamp` fully authored, `agile-scrum-master-product-owner` still skeleton-only)
Owner asked to build skeletons first, content later, shorter course first. Two `Course` rows:
1. `agile-scrum-master-product-owner` — «اجایل و اسکرام برای اسکرام مستر و مالک محصول», 105 lessons across 12 sections (مقدمه و چرا اسکرام → مبانی اجایل → محصول → تجربه‌گرایی و چارچوب اسکرام → رویدادهای اسکرام → تم/اپیک/یوزر استوری → آرتیفکت‌ها → EBM و گواهی‌نامه → مسیر شغلی اسکرام مستری → روزمرگی و چالش‌های اسکرام مستر → شروع کار به‌عنوان PO/اسکرام مستر → جمع‌بندی).
2. `scrum-master-ai-bootcamp` — «بوت‌کمپ هوش مصنوعی برای اسکرام مستر», 32 lessons across 4 sections (مبانی هوش مصنوعی مولد و پرامپت‌نویسی → بک‌لاگ و نیازمندی‌ها با کمک هوش مصنوعی → اجرای اسپرینت با کمک هوش مصنوعی → ابزارهای هوش مصنوعی در کار روزمره).

**`scrum-master-ai-bootcamp` fully authored 2026-09-17 (32 lessons, 87 practices, 4 sections, Persian-only, one pass per section):** owner picked this one first since it's the shorter of the two. Sections: «مبانی هوش مصنوعی مولد و پرامپت‌نویسی» (10 lessons — tools, memory/tokens/context window, custom GPTs, COSTAR/RISE/RTF, prompt chaining/meta-prompting/chain-of-thought, an AI-output validation framework), «بک‌لاگ و نیازمندی‌ها با کمک هوش مصنوعی» (6 — an end-to-end case study from a vision doc through Perplexity market research, backlog generation, epic/story refinement, DoD/DoR, acceptance test cases, wired into a real Jira workflow incl. a Claude-to-Jira connector), «اجرای اسپرینت با کمک هوش مصنوعی» (6 — MoSCoW/story-point estimation, sprint capacity/backlog/sequencing planning, impediment detection+resolution from raw stand-up notes, burndown forecasting with visualization, plus a dense closing lesson covering all three remaining ceremonies at once since the source video did), «ابزارهای هوش مصنوعی در کار روزمره» (10 — ChatGPT Agent Mode, connectors, Microsoft Copilot across Word/Excel/PowerPoint/Outlook, ChatGPT Projects, Mermaid diagrams, Otter.ai meeting scribing, Claude-driven sprint dashboards and QBR/MBR reports via Gamma.app). `validate.py` fully clean (0 warnings, 0 problems); `content:import` loads 32/87; `CourseImporterTest` 10/10; full suite 123/123. `agile-scrum-master-product-owner` (105 lessons) is next, still skeleton-only.

Both source folders had a real gotcha: every `.srt` file (not just the video files) had been saved under a literal broken filename — `index.html?token=...&hash=...&filename=<real name>.srt` — from a failed download that kept the URL query string instead of the real filename; the subtitle *content* itself was intact. Fixed by stripping everything up to `filename=` and renaming before the normal `srt2vtt.py` pass; also deleted a stray `links` file (a leftover CDN URL list, not course content) from each folder. Otherwise followed the normal §7 "new course" flow: renamed media to `NNN-topic-hash.ext` (kept the source's own 4-char hash suffix for slug uniqueness, same convention as `advanced-scrum-master`'s `-kdcq`/`-qykx` style suffixes — several lecture titles repeat across sections, e.g. two "Module Case Study Introduction and Goal Setting" in the AI bootcamp), converted subtitles, symlinked `public/media/<slug>`, wrote real (not placeholder) Persian titles/summaries per lesson straight into the skeleton — matching how `advanced-scrum-master`'s skeleton was already done, not bare English stand-ins. `estimated_minutes` computed from each `.mp4`'s real duration via `ffprobe`. `validate.py` clean for both (only the expected stub/no-key_points/no-practices "problems" every unauthored skeleton has, 0 warnings); `content:import` loads both (105 / 32 lessons, 0 practices each — expected pre-content); `CourseImporterTest` 10/10. Content authoring (lesson text + practices, section by section) not started yet — next up whenever the owner says so.

**Skeletons for all 9 remaining courses built 2026-09-17 (before continuing `requirements-engineering`'s own content authoring, at the owner's request — batch all skeletons now rather than one at a time as authoring reaches each course):** same process as `requirements-engineering`'s own skeleton below — unwrap `index.html?...&filename=X` artifacts, rename to `NNN-kebab-title.{mp4,en.srt,fa.srt}`, `srt2vtt.py`, `ffprobe` durations, sections inferred from each course's own lecture-title patterns (intro/summary/recap markers) without watching video. All 9 now have real (if not-yet-deep) Persian titles/summaries, `content:import` loading cleanly with 0 practices/key_points (expected pre-authoring): `software-architecture-complete-guide` (113 lessons/17 sections), `system-design-case-studies` (19/7), `large-scale-systems-design` (41/11), `becoming-a-product-manager` (109/12), `advanced-scrum-master` (59/9), `practical-guide-for-tech-leaders` (34/7), `become-an-engineering-manager` (66/8), `organizational-leadership-ai-era` (15/3), `generative-ai-in-organizations` (17/5). One recurring issue caught and fixed across several courses: multiple lectures titled just "Summary" or "Conclusion" within the same course produced duplicate lesson slugs — disambiguated by section topic (e.g. `summary-application-types`, `conclusion-requirements`); `software-architecture-complete-guide` alone had 10 such collisions. `course/Management course/` (the original bulk-dropped folder) is now empty and removed now that all 10 sub-courses have their own top-level `course/<slug>/` folder; `course/scrum course/` remains untouched, still out of scope. Full details in `docs/DECISIONS.md` §57. **Deploying these skeletons to the live host is the owner's own next step, not done by this session.**

**`requirements-engineering` skeleton done (2026-09-16):** moved `course/Management course/1-Requirements-Engineering/` → `course/requirements-engineering/` (own top-level folder, matching the other two courses' convention) and symlinked into `public/media/`. Source filenames were already `NNN-kebab-title-XXXX-git.ir[.fa].{mp4,srt}` (cleaner than C-02's raw filenames, no full rename script needed) plus the usual `index.html?token=...&filename=...` download-tool artifacts wrapping every `.srt` (172 of them) — unwrapped first, then stripped the `-XXXX-git.ir` tag and re-kebabbed the title, then `srt2vtt.py` (no empty subtitles this time, unlike C-01's 9). `content/requirements-engineering/course.json` has all **86 lessons** across 8 sections — «مقدمه و مبانی» (18), «سناریو، User Story و Use Case» (9), «استخراج نیازمندی‌ها (Elicitation)» (13), «نوشتن و فرایند نیازمندی‌ها» (6), «نیازمندی‌های نرم‌افزار» (10), «مستندسازی و مدل‌سازی نیازمندی‌ها» (15), «مذاکره و مدیریت تعارض» (8), «مدیریت، ردیابی و جمع‌بندی» (7) — real (if not-yet-deep) Persian titles/summaries from the lecture titles, `estimated_minutes` from actual `ffprobe` durations (~17 hours of source video total), one-paragraph placeholder `.md` per lesson, no practices/key_points yet. First lesson renamed to `course-overview` per the established exemption; last renamed to `course-closeout` (not yet decided whether it gets real practices or needs its own exemption — the video is a pure wrap-up, unlike C-02's `pydoc` closer which still taught something). One real content oddity found and preserved as-is pending a decision at authoring time: lectures 031 and 036 are both literally titled "Workshops" with different narration (031 formal/lecture-style, 036 scenario-style) — disambiguated as `requirements-elicitation-techniques-workshops` / `...-workshops-ii` for now; may turn out to be a genuine source duplicate worth merging once actually read. `content:import` loads cleanly (86 lessons, 0 practices, as expected pre-authoring); `validate.py` reports 257 expected problems (stub text + missing practices/key_points on all 85 non-overview lessons) — normal at skeleton stage, cleared section by section same as C-01/C-02.

**«مقدمه و مبانی» پاس ۱/۲ authored 2026-09-16** — first 9 lessons (course-overview, why-requirements, examples-used-in-this-course, definitions-terms, product-life-cycle-overview, users-stakeholders, the-rfp-development-contract, system-environment-context, the-system-hierarchy — 23 practices). Follows the video's own examples throughout: the ATM + tree-seed drone (established in `examples-used-in-this-course` as the course's two running systems) for most lessons, but kept the video's own jet-fighter/car examples for `system-environment-context` and `the-system-hierarchy` where those were genuinely what the source used (per AUTHORING.md's "same examples as the video" rule). `definitions-terms` is deliberately a light glossary pass — most of its terms (stakeholder, system environment, system hierarchy, systems engineering) get their own dedicated lesson later in this same section, so it only defines them in 1–2 sentences and doesn't re-teach them. Caught one internally inconsistent rule in the source transcript for `system-environment-context`: the video states a boundary rule ("if it needs custom development for your system, it's inside your boundary") then contradicts it by listing custom-built simulators/tools/test-facilities as *outside* the boundary — this is actually standard systems-engineering doctrine (enabling/support systems are conventionally a separate system from the end item, even when custom-built), but the video's phrasing doesn't make that distinction explicit. Sidestepped the contradiction rather than teaching it: the lesson only presents the internally-consistent half of the rule (shared/unmodified things are outside) and doesn't test the confusing part in practices. `validate.py requirements-engineering` now reports 231 problems (down from 257 — exactly the expected drop for 9 lessons losing their stub/missing-practices/no-key_points flags), `content:import` loads 86 lessons / 23 practices, full suite 120/120.

**«مقدمه و مبانی» پاس ۲/۲ authored 2026-09-16 — section complete (18 lessons, 49 practices).** Remaining 9 lessons: system-requirements, systems-engineering-overview, requirement-types-part-1, requirements-origination, the-requirements-engineer, requirements-engineering (the process roadmap), product-needs-goals, the-mission-need-statement-mns, identifying-project-constraints — 26 more practices. `requirements-engineering` (the process-overview lesson) is deliberately a light roadmap pass for the same reason as `definitions-terms` in pass 1: `product-needs-goals`, `the-mission-need-statement-mns` and `identifying-project-constraints` right after it go deep on the steps it only previews. `system-requirements` similarly stays light on the specification outline/tree, since the whole «مستندسازی و مدل‌سازی نیازمندی‌ها» section later covers that in full. Caught 3 mcq practices where the rubric's stated option number didn't match `correct_option` (wrote the 1-indexed option position instead of the 0-indexed value the validator/grader actually checks against) — `validate.py` caught all 3 directly; also wrote a one-off script to scan every mcq in the course for the same class of mismatch and confirmed no others existed. `validate.py requirements-engineering` now reports 204 problems (down from 231, exactly 9 lessons' worth), `content:import` loads 86 lessons / 49 practices, full suite 120/120. **Section «مقدمه و مبانی» is now fully authored: all 18 lessons, 49 practices.**

**English text backfilled for all 18 lessons (2026-09-16, DECISIONS.md §52).** `<slug>.en.md` written for every lesson in this section, retroactive per owner request. Every lesson of C-03 gets both from here on.

**«سناریو، User Story و Use Case» section complete 2026-09-16 (9 lessons, 26 practices, with English text from the start per §3.2a).** All 9 lessons authored in one pass: `introduction-to-scenarios`, `scenarios`, `scenario-types`, `scenario-content`, `scenario-formatting`, `scenario-templates`, `rules-of-thumb-for-scenario-creation`, `user-stories`, `use-cases` (both `.md` and `.en.md`, the latter written from the transcript directly, not translated). Threaded the course's two running examples (ATM, tree-seed-planting drone) throughout, matching the video's own usage. `use-cases` (027) is the section's heaviest lesson at ~30 minutes of source — covers use case elements, the UML diagram notation (association/multiplicity/generalization/include/extend), and the full ATM withdraw-cash walkthrough including the include-relationship pattern for shared Initialize/Finalize Transaction use cases. Dropped an inaccurate "this lesson has two videos" opening blockquote initially copy-pasted from the style guide's example — `course.json` shows `use-cases` as a single video file, caught before commit. `validate.py requirements-engineering` reports only the pre-existing stub/missing-practice problems for lessons beyond this section (178, matching 86 − 9 newly-clean − 18 already-clean lessons × ~3 problems each) plus the known `.en.md`-files-not-in-course.json validator limitation (harmless, same as section 1); `content:import` loads 86 lessons / 75 practices; `CourseImporterTest` 10/10.

**English lesson text discontinued 2026-09-17 (DECISIONS.md §54).** Owner ended the §52 experiment: the two sections above keep their `.en.md` files, but every lesson from here on — the rest of this course and every future course — is Persian-only again, no `<slug>.en.md`. `docs/AUTHORING.md` §3.2a updated to reflect this.

**«استخراج نیازمندی‌ها (Elicitation)» پاس ۱/۲ authored 2026-09-17 (7 lessons, 21 practices, Persian-only — first section under the §54 Persian-only rule).** Lectures 028–034: `introduction-to-requirements-elicitation`, `requirements-elicitation-preparation`, `requirements-elicitation-techniques-interviews`, `requirements-elicitation-techniques-workshops`, `requirements-elicitation-techniques-questionnaires`, `requirements-elicitation-techniques-document-analysis`, `requirements-elicitation-techniques-observation`. Kept the course's mining analogy and ATM/tree-seed-drone examples running throughout. `requirements-elicitation-techniques-workshops` (031) is the formal/lecture-style "Workshops" lecture (planning, moderator role, RFI/RFA, closeout) — the skeleton-stage note about 031 vs. 036 both being titled "Workshops" still needs resolving in pass 2 once 036's transcript is actually read; 031's content reads as a complete, general treatment of the technique, so 036 may turn out to be a worked example/case-study rather than a duplicate, but that's unconfirmed. Local dev DB note: `skillos-mysql` Docker container (port 3307) was stopped at the start of this session — `docker start skillos-mysql` before `content:import` if it happens again. `validate.py requirements-engineering` clean for these 7 (only `requirements-elicitation-techniques-workshops-ii`, the untouched pass-2 stub, still flags); `content:import` loads 86 lessons / 96 practices; `CourseImporterTest` 10/10.

**«استخراج نیازمندی‌ها (Elicitation)» پاس ۲/۲ authored 2026-09-17 — section complete (12 lessons, not 13 — see below; 43 practices this pass).** Lectures 035, 037–040: `requirements-elicitation-techniques-working-groups`, `requirements-elicitation-tools-brainstorming`, `requirements-elicitation-tools-the-kj-method`, `requirements-elicitation-tools-mind-mapping`, `requirements-elicitation-tools-prototyping`. **Resolved the 031/036 "Workshops" duplicate flagged since the skeleton stage:** read both full transcripts — they're the same lecture re-recorded (same structure, same drone example, overlapping sentences), not two distinct lessons. Merged lecture 036 into the existing `requirements-elicitation-techniques-workshops` lesson as a second video rather than authoring a near-duplicate lesson; removed the standalone `requirements-elicitation-techniques-workshops-ii` lesson from `course.json` and pruned it from the DB. Logged in `docs/DECISIONS.md` §55. Net effect: this section has **12 lessons** (13 lecture numbers, 028–040), not the 13 estimated at skeleton time. `validate.py requirements-engineering` clean for the whole section; `content:import --prune` loads 85 lessons / 111 practices; `CourseImporterTest` 10/10. Local dev DB note carried over from pass 1: start `skillos-mysql` Docker container if `content:import` refuses to connect.

**Section «استخراج نیازمندی‌ها (Elicitation)» is now fully authored: 12 lessons (both passes combined), 64 practices.**

**«نوشتن و فرایند نیازمندی‌ها» section complete 2026-09-17 (6 lessons, 20 practices, Persian-only).** Lectures 041–046: `requirements-writing-introduction`, `requirements-engineering-process`, `requirement-statements`, `requirements-derivation-decomposition-flow-down`, `defining-measures-of-effectiveness-moes`, `requirement-types-part-2`. `requirement-statements` (043) is by far the heaviest lesson in the course so far — ~40 minutes of source covering the full shall-statement grammar (condition/subject/priority/verb/object/qualification), the glossary/capitalization convention, all the single-sentence/no-conjunction/active-voice/definite-article/quantifiable/threshold-objective/devil's-advocate rules, and the 4 specification-set-level characteristics (complete, consistent, feasible, validated) — got 6 practices instead of the usual 2-3, proportional to its depth. Threaded the drone/ATM examples throughout, including full derivation → decomposition → flow-down and MOE → MOP worked examples for the tree-seed drone. `validate.py requirements-engineering` clean for the section; `content:import` loads 85 lessons / 131 practices; `CourseImporterTest` 10/10.

**«نیازمندی‌های نرم‌افزار» section complete 2026-09-17 (10 lessons, 34 practices, one pass, Persian-only).** Lectures 047–056: `software-requirements-introduction`, `the-software-development-process`, `identifying-software-requirements`, `analyzing-software-requirements`, `validating-software-requirements`, `software-requirements-management`, `requirements-prioritization`, `technical-performance-measures-tpms`, `requirements-verification`, `requirements-validation`. Three of these (054–056, TPMs/verification/validation) run ~35 minutes of source each — the densest trio in the course after `requirement-statements` (043) — and got 5-6 practices apiece instead of the usual 2-3, proportional to depth, same call as §043. Threaded the course's running ATM and tree-seed-drone examples throughout (ATM for the software-specific lessons per the video's own choice, drone for TPM/KPP/verification/validation worked examples). `validate.py requirements-engineering` clean for the section; hit one local-only snag — a stale `bootstrap/cache/config.php` from an earlier `/_ops/optimize` test run pinned `DB_CONNECTION` to sqlite despite `.env` saying mysql; `php artisan config:clear` fixed it, nothing content-related. `content:import` loads 85 lessons / 165 practices; full suite 123/123.

**«مستندسازی و مدل‌سازی نیازمندی‌ها» پاس ۱/۲ authored 2026-09-17 (8 lessons, 22 practices, Persian-only).** Lectures 057–064: `requirements-documentation-specifications-overview`, `the-requirements-specification` (BRS/STRS/SYRS chain, ~25 min source, the densest lesson this pass), `specification-standards` (complete/consistent/flexible), `specification-templates` (MIL-STD-961E section-by-section), `requirements-modeling-introduction`, `requirements-modeling-tools`, `the-entity-relationship-er-diagram`, `the-class-diagram` — the last two start the MBSE-modeling half of this section, threaded through the ATM example per the video's own choice. `validate.py requirements-engineering` clean for these 8; `content:import` loads 85 lessons / 187 practices; `CourseImporterTest` 10/10.

**«مستندسازی و مدل‌سازی نیازمندی‌ها» پاس ۲/۲ authored 2026-09-17 — section complete (15 lessons total, 22+20=42 practices, Persian-only).** Lectures 065–071: `the-data-flow-diagram-dfd`, `the-activity-diagram-functional-flow`, `the-sequence-diagram`, `the-state-machine-diagram`, `the-block-definition-internal-block-diagram`, `the-requirements-diagram`, `the-statement-of-work-sow`. This half of the section covers the MBSE-modeling toolkit (DFD, activity, sequence, state machine, BDD/IBD, SysML requirements diagrams) plus the statement-of-work specification type, all threaded through the ATM example per the video's own choice. `validate.py requirements-engineering` clean for these 7; `content:import` loads 85 lessons / 207 practices; full suite 123/123. **Section «مستندسازی و مدل‌سازی نیازمندی‌ها» is now fully authored: 15 lessons, 42 practices.**

**«مذاکره و مدیریت تعارض» section complete 2026-09-17 (8 lessons, 20 practices, one pass, Persian-only).** Lectures 072–079: `common-requirements-issues` (too-few requirements, requirements creep, stove-piping, undefined system/project boundaries with the helicopter-engine-upgrade example, confused stakeholders, developer misinterpretation, unverifiable requirements), `requirements-negotiation-overview` (8-step process, risks/pitfalls), `requirements-negotiation-strategies` (team-building, scope definition, common goals, prioritization, open communication), `requirements-communication-documentation` (active listening, clear language, regular check-ins, visual aids), `documenting-changes` (why + the standard change-request process), `managing-conflicts` (the section's heaviest lesson, ~24 min source — RE-vs-non-RE conflict triage, the six IREB CPRE conflict types with tree-seed-drone examples for each including the Mars Climate Orbiter units-mismatch story for data conflict, and the six resolution techniques with worked examples for each; got 5 practices instead of the usual 2-3, proportional to depth, same call as §043/§054-056), `managing-difficult-stakeholders` (root causes + strategies), `requirements-negotiation-tips` (10 tips + module closeout). Threaded the course's ATM and tree-seed-drone running examples throughout, matching each video's own choice. `validate.py requirements-engineering` clean for these 8 (only the pre-existing section-8 stub/`.en.md` issues remain, unrelated to this section); `content:import` loads 85 lessons / 227 practices; `CourseImporterTest` 10/10; full suite 123/123.

**«مدیریت، ردیابی و جمع‌بندی» section complete 2026-09-17 — course fully authored (7 lessons, 17 practices, one pass, Persian-only).** Lectures 080–086: `requirements-management-overview` (module intro/recap), `managing-requirements-artifacts` (metadata, traceability, configuration management, what drives change), `managing-requirements-processes` (monitor/identify/control cycle, per-lifecycle-phase input/output expectations, situational process selection for agile), `product-develpment-technical-reviews` (event-driven vs. schedule-driven reviews, the full DoD review chain ASR→SRR→SFR→PDR→CDR→TRR→PRR→OTRR→PCA, entry/exit criteria, RFI/RFA, third-party chair), `the-system-requirements-review-srr` (the deepest dive of the section — pre-award timing, entry criteria, meeting flow, "specification becomes law" post-award, exit criteria; got 5 practices instead of the usual 2-3, proportional to depth, same call as §043/§054-056/managing-conflicts), `requirements-traceability` (why trace, gold-plating detection via over-allocation, traceability matrices, the full need→goal→scenario→use-case→requirement→function→component chain), `course-closeout` (the toolbox metaphor, course wrap-up). Threaded the course's ATM and tree-seed-drone running examples throughout. Hit one local-only snag — stale `bootstrap/cache/config.php` pinning `DB_CONNECTION` to sqlite again; `php artisan config:clear` fixed it. `validate.py requirements-engineering` fully clean (only the pre-existing harmless `.en.md`-files-not-in-course.json validator note remains, same as section 1/2); `content:import` loads all 85 lessons / 244 practices; `CourseImporterTest` 10/10; full suite 123/123.

**`software-architecture-complete-guide` content authoring started 2026-09-17 — owner asked for a text-only pass first (lesson `.md` bodies only, no practices/key_points this round; those come in a later pass, matching how `requirements-engineering` was originally scoped before C-03's fuller depth was chosen).** «مقدمه» section complete (4 lessons): `course-overview`, `who-is-this-course-for`, `why-you-should-become-a-software-architect`, `what-we-will-talk-about-in-this-course`. All read from the English `.srt`/`.vtt` transcripts (this course has no slides/notebooks, video-only). `validate.py software-architecture-complete-guide` no longer flags these 4 as stubs (missing-practices/no-key_points warnings on them are expected and will clear once the practices pass happens); `content:import` loads 113 lessons / 0 practices; `CourseImporterTest` 10/10. 16 sections / 109 lessons remain for the text pass.

**«نقش معمار نرم‌افزار» section complete 2026-09-17 (6 lessons, text-only pass).** `types-of-architects`, `responsibility-of-the-architect`, `the-architect-in-the-organizational-chart`, `architects-code`, `architects-and-academic-degrees`, `career-path-to-architect`. Caught and fixed a skeleton-stage title/summary mismatch: `architects-code` (lecture 010) was titled «منشور اخلاقی معماران» (code of ethics) at skeleton time going only off the filename, but the actual video is about whether an architect should write code — retitled to «آیا معمار باید کد بزنه؟» with a matching summary in `course.json`. `validate.py` clean of stubs for these 6; `content:import` loads 113/0; `CourseImporterTest` 10/10.

**«طرز فکر معمار» section complete 2026-09-17 (5 lessons, text-only pass).** `introduction-to-the-architect-s-mindset`, `understand-the-business`, `define-the-system-s-goals`, `work-for-your-client-s-clients`, `talk-to-the-right-people-with-the-right-language` — the core mindset-shift section (business understanding, system goals vs. requirements, "work for your client's client", audience-adapted communication with the Sophie/Dave/Shelly examples kept as-is from the video). `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10.

**«فرایند طراحی معماری» section complete 2026-09-17 (9 lessons, text-only pass).** `introduction`, `understand-the-system-requirements`, `understand-the-non-functional-requirements`, `map-the-components`, `select-technology-stack`, `design-the-architecture`, `write-the-architecture-document`, `support-the-team`, `conclusion-architecture-process`. Short roadmap-style lectures (source transcripts 6–17 lines each) previewing the 7-step process that the rest of the course deep-dives section by section; lessons stayed correspondingly brief, matching the video's own pacing rather than padding. `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10.

**«نیازمندی‌ها از دید معمار» section complete 2026-09-17 (6 lessons, text-only pass).** `introduction-to-requirements`, `the-two-types-of-requirements`, `architects-functional-requirements`, `non-functional-requirements`, `who-defines-non-functional-requirements`, `conclusion-requirements`. `non-functional-requirements` (033) is this section's heaviest lesson (~125-line transcript) — full walkthrough of performance (latency vs. throughput, with worked numbers), load vs. throughput, data volume, concurrent users (the 10x-of-concurrent-requests rule of thumb), and SLA (kept the "3 data centers with dual power and automatic failover" anecdote as-is). `validate.py` clean of stubs for these 6; `content:import` loads 113/0; `CourseImporterTest` 10/10. 12 sections / 78 lessons remain for the text pass.

**«انواع اپلیکیشن» section complete 2026-09-17 (8 lessons, text-only pass).** `introduction-to-application-types`, `web-apps`, `web-api`, `mobile`, `console`, `service`, `desktop`, `summary-application-types` — the six mainstream types (web app, web API/REST, mobile, console/CLI, service, desktop) with each lesson's own "best for" checklist kept as the video structured it. `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10. 38/113 lessons done, 11 sections / 75 lessons remain.

**«انتخاب پشته‌ی فناوری» section complete 2026-09-17 (6 lessons, text-only pass).** `introduction-to-technology-stack`, `considerations-for-technology-stack`, `back-end-technology`, `front-end-technology`, `data-store-technology`, `summary-technology-stack`. The two heaviest lessons of the course so far: `back-end-technology` (.NET/.NET Core/Java/Node.js/PHP/Python comparison with a summary table) and `front-end-technology` (Angular vs. React, native/hybrid/cross-platform mobile with a comparison table, WinForms/WPF/UWP desktop with a comparison table) — both got an opening blockquote flagging the recording's age (pre-.NET 5, PWA-as-emerging-standard) since some popularity/maturity claims are dated even though the decision framework itself still holds. `data-store-technology` covers SQL vs. NoSQL concepts (not specific products) including ACID, eventual consistency, and the blurring-line note (MongoDB adding ACID, SQL Server's JSON querying). `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10. 44/113 lessons done, 10 sections / 69 lessons remain.

**«ایلیتی‌ها (ویژگی‌های کیفی سیستم)» section complete 2026-09-17 (7 lessons, text-only pass).** `introduction-to-the-ilities`, `scalability` (scale-up vs. scale-out with the redundancy/limit reasoning), `manageability` (who reports the problem — the user or the system itself), `modularity`, `extensibility` (the JSON/XML/CSV formatter example, with a short representative code snippet reconstructed from the video's on-screen-only code since the transcript only described it verbally), `testability` (unit vs. integration testing, the two testability traits), `summary-ilities`. `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10. 51/113 lessons done, 9 sections / 62 lessons remain.

**«طراحی اجزا (Components)» section complete 2026-09-17 (9 lessons, text-only pass) — densest section of the course so far (425-line combined transcript).** `introduction-to-components`, `layers` (UI/SI–BL–DAL, the three concepts of strict code flow / loose coupling / exception encapsulation between layers, layer vs. tier), `interfaces`, `di` (factory method and constructor injection), `solid` (all five principles with the mail-sender Liskov example and the bloated-interface segregation example), `naming-conventions`, `exception-handling`, `logging` (kept the 2am-phone-call framing story), `summary-components`. `layers`/`interfaces`/`di` reconstruct short illustrative C#-style code snippets for patterns the video showed on-screen only (transcript describes them verbally, e.g. "new is glue") — no exact source code existed to transcribe, so these are representative, not verbatim. `validate.py` clean of stubs (had to pad `summary-components` past the 500-byte stub threshold); `content:import` loads 113/0; `CourseImporterTest` 10/10. 60/113 lessons done, 8 sections / 53 lessons remain.

**«الگوهای طراحی» section complete 2026-09-17 (6 lessons, text-only pass, 266-line combined transcript).** `introduction-to-design-patterns` (Gang of Four background, why an architect should care), `the-factory-pattern` (HotOrNotWeather/EtherNeatherWeather story), `the-repository-pattern` (IEmployeesRepository example, the column-rename and SQL-Server-to-MongoDB-switch scenarios), `the-facade-pattern` (bank transfer example), `the-command-pattern` (Word's undo mechanism, ICommand/receiver/invoker), `summary-design-patterns`. All four pattern lessons reconstruct illustrative C#-style code from the video's on-screen-only examples (transcript describes the code verbally, same approach as the components section). `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10. 66/113 lessons done, 7 sections / 47 lessons remain.

**«معماری سیستم» section complete 2026-09-17 (7 lessons, text-only pass, 526-line combined transcript — the heaviest section so far, and the one the video itself calls "the most important section in this course").** `introduction-to-system-architecture`, `loose-coupling` (spider-web problem, Yellow Pages directory vs. gateway/middleman solutions), `stateless` (the login-across-load-balanced-servers walkthrough, why stateful breaks scalability and redundancy), `caching` (in-memory/in-process vs. distributed, the stock-quotes stale-cache scenario, a comparison table), `messaging` (REST API vs. push notification/Pub-Sub vs. queue vs. file/database polling, each scored against performance/size/execution-model/feedback/complexity, comparison table — this course's single densest lesson), `logging-monitoring` (central logging service, correlation ID), `summary-system-architecture`. `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10. 73/113 lessons done, 6 sections / 40 lessons remain.

**«ملاحظات بیرونی» section complete 2026-09-17 (5 lessons, text-only pass, short section — 75-line combined transcript).** `introduction-to-external-considerations`, `deadline` (the Douglas Adams quote), `existing-dev-team-skills` (.NET-to-Java vs. Windows-desktop-to-Python migration difficulty contrast), `it-support` (don't turn developers into DBAs/product support — the "excitement fades" warning), `cost` (buy-vs-build with the $100k/200-features/5-needed example). `validate.py` clean of stubs (padded the short intro lesson past the 500-byte threshold); `content:import` loads 113/0; `CourseImporterTest` 10/10. **78/113 lessons done, 5 sections / 35 lessons remain — owner asked to stop here for this session; resume with «نوشتن سند معماری» (10 lessons) next.**

**«نوشتن سند معماری» section complete 2026-09-18 (10 lessons, text-only pass, 357-line combined transcript).** `introduction-to-architecture-document`, `goal-of-the-document`, `audience` (dev team / management / QA, each explained), `contents-of-the-document` (why the course avoids UML — plain language over diagrams), `document-s-structure` (the 5-part roadmap), `background-overview-section`, `requirements-section`, `executive-summary-section` (written last, appears first — the "don't repeat yourself" and sparse-technical-terms tips), `architecture-overview-section` (the 3-part structure: general description, high-level logical diagram, walkthrough), `components-drill-down-section` (the 4 subsections per component: role, tech stack with rationale, component architecture, development instructions). Local dev DB note: `skillos-mysql` Docker container had stopped since the prior session — `docker start skillos-mysql` fixed `content:import`'s connection-refused error; unrelated to content. `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10. **88/113 lessons done, 4 sections / 25 lessons remain: «مطالعه‌ی موردی: IOToo» (11), «معماری‌های پیشرفته» (5), «مهارت‌های نرم برای معماران» (8), «جمع‌بندی دوره» (1).**

**«مطالعه‌ی موردی: IO2» section complete 2026-09-18 (11 lessons, text-only pass, 700-line combined transcript — biggest section by transcript volume).** Caught and fixed a skeleton-stage mishearing: the fictional company is **IO2** ("eye-oh-two"), not "IOToo" — fixed the section name and all lesson titles/summaries in `course.json` before writing. Lessons: `introduction-to-case-study`, `presenting-iotoo` (slug kept as-is, no practices/URLs depend on it yet), `defining-the-requirements` (full non-functional-requirements walkthrough with worked numbers — 500 concurrent messages, 15M/month, 54GB/year data volume, 99% message-loss tolerance, the silver/gold/platinum SLA-tiers method), `mapping-the-components` (Receiver/Handler/Info/central-logging split, why validation+parsing stay one component), `choosing-messaging-methods` (REST for Receiver, queue for Receiver→Handler, REST for Info, queue for logging — with the file-based-logging pitfalls), `designing-the-logging-service`, `designing-the-receiver` (queue-handling layer replacing DAL, cross-cutting logging layer, NFR compliance check), `designing-the-handler`, `designing-the-info-service` (REST path design for nested entities, query-string vs. path-segment reasoning, response-code table), `writing-the-architecture-document`, `get-the-architecture-document` — this last one adapted honestly: this course has no `files/` attachments at all (confirmed — no slides/notebooks for the whole course), so the lesson explains the source video's downloadable-template offer but is upfront that the actual file isn't attached in this catalog, pointing instead to the structure walked through across the section as the reusable roadmap. `validate.py` clean of stubs; `content:import` loads 113/0; `CourseImporterTest` 10/10. Local dev DB note: `skillos-mysql` Docker container had stopped again — `docker start skillos-mysql`. **99/113 lessons done, 3 sections / 14 lessons remain: «معماری‌های پیشرفته» (5), «مهارت‌های نرم برای معماران» (8), «جمع‌بندی دوره» (1).**



**`software-architecture-complete-guide` text-only pass complete 2026-09-18 — all 113 lessons authored, course fully readable end to end.** Final lesson: `course-closeout` (the "چی یاد گرفتیم؟" wrap-up, walking back through every section of the course). Also fixed 2 validator warnings caught only at the very end — Persian inside code comments in `layers.md` and `di.md` (from the components section, several sections back) — switched to English comments per §5's rule. `validate.py software-architecture-complete-guide` now fully clean: **0 warnings, 0 stub-text problems** across all 113 lessons (the remaining 225 "problems" are 113×~2 expected missing-practices/no-key_points, since this pass deliberately deferred both per the owner's scoping decision at the start of this work). `content:import` loads 113 lessons / 0 practices; **full suite 123/123 passed, 564 assertions.** Section-by-section recap of this pass: مقدمه (4) → نقش معمار نرم‌افزار (6, with an architects-code title fix) → طرز فکر معمار (5) → فرایند طراحی معماری (9) → نیازمندی‌ها از دید معمار (6) → انواع اپلیکیشن (8) → انتخاب پشته‌ی فناوری (6) → ایلیتی‌ها (7) → طراحی اجزا (9) → الگوهای طراحی (6) → معماری سیستم (7, the course's own "most important section") → ملاحظات بیرونی (5) → نوشتن سند معماری (10) → مطالعه‌ی موردی: IO2 (11, with an IOToo→IO2 company-name fix) → معماری‌های پیشرفته (5) → مهارت‌های نرم برای معماران (8) → جمع‌بندی دوره (1) = 113. **Next up when the owner wants it: the practices+key_points pass (2–3 practices/lesson, English rubrics, key_points/common_mistakes in course.json) — same depth as C-01/C-02/C-03, per the original owner instruction to do a lighter text-first pass and add practices later.**

**`software-architecture-complete-guide` practices+key_points pass started 2026-09-18 (owner asked for a timing check first, one section, then continue).** «مقدمه» (4 lessons, 6 practices — `course-overview` stays practice-free per the established intro-lesson exemption) and «نقش معمار نرم‌افزار» (6 lessons, 13 practices) both done, 19 practices total after those two. «طرز فکر معمار» (5, 9 practices), «فرایند طراحی معماری» (9, 12), «نیازمندی‌ها از دید معمار» (6, 10 — non-functional-requirements got 3 practices as the section's heaviest lesson), «انواع اپلیکیشن» (8, 12), «انتخاب پشته‌ی فناوری» (6, 13 — back-end/front-end/data-store-technology each got 3) all done — **75 practices total across 34 lessons**. `validate.py` clean for all; `content:import` loads 113 lessons / 75 practices; `CourseImporterTest` 10/10; full suite 123/123. **10 sections / ~79 lessons remain for this pass**, starting with «ایلیتی‌ها» (7 lessons) next.
**«مهارت‌های نرم برای معماران» section complete 2026-09-18 (8 lessons, text-only pass, 228-line combined transcript — last content section before the course's single closing lesson).** `introduction-to-soft-skills` (influence without authority), `listening`, `dealing-with-criticism` (the "John" anecdote — a disgruntled would-be chief architect), `be-smart-not-right` (the CTO/stateless meeting-approval scenario, full suggested response script), `organizational-politics` (the CIO-replacement/microservices story), `public-speaking`, `learning` (DZone/InfoQ/O'Reilly, the jQuery/Grunt/AngularJS/Hadoop obsolescence examples), `summary-soft-skills`. `validate.py` clean of stubs for all 8; `content:import` loads 113/0; `CourseImporterTest` 10/10. **112/113 lessons done — only `course-closeout` (the single closing lesson of the whole course) remains for the text-only pass.**
**«معماری‌های پیشرفته» section complete 2026-09-18 (5 lessons, text-only pass, 196-line combined transcript).** `introduction-to-advanced-architectures`, `micro-services` (monolith pain points → the 4 microservices fixes, Netflix's 700-service/Eureka example, the 3-month/500-line design-cost anecdote), `event-sourcing` (bank-statement analogy, pros/cons), `cqrs` (as the fix for event sourcing's query weakness — two databases + sync engine), `summary-advanced-architectures`. `validate.py` clean of stubs (padded the short summary lesson past 500 bytes). `content:import` loads 113/0; `CourseImporterTest` 10/10. **104/113 lessons done, 2 sections / 9 lessons remain: «مهارت‌های نرم برای معماران» (8), «جمع‌بندی دوره» (1) — the course's last section.**

**Course `requirements-engineering` is now 100% authored: 8 sections, 85 lessons, 244 practices.** No further sections remain for this course.

### [~] C-05 Retire essay-style practices, starting with `requirements-engineering` (owner rule 2026-09-22, DECISIONS.md §64)
Owner rule, general going forward: no practice should require a multi-sentence answer. `explanation`/`scenario` forms and any overlong `short_answer` get rewritten to true short answers (1–2 words, multiple short blanks OK). `requirements-engineering` first: 155 of 244 practices need rewriting (73 explanation + 42 scenario + 40 overlong short_answer), section by section, one commit per section. Other courses (`complete-python-mastery`, `python-deep-dive-1`, `software-architecture-complete-guide`, etc.) keep their existing `explanation`/`scenario` practices until a later session migrates them — not in scope here.

**«مقدمه و مبانی» done 2026-09-22 (17 lessons with practices, 34 explanation/scenario practices rewritten to true short_answer).** Also fixed a pre-existing bug found along the way: `the-system-hierarchy`'s `multiplicity-scenario` practice had `form: "scenario"` but was actually mcq-shaped (`options`/`correct_option`, rubric "Correct only if option 1 is selected") — the textarea UI would've shown no options at all. Changed its form to `mcq`, content unchanged. `validate.py requirements-engineering` clean (only the pre-existing harmless `.en.md`-not-in-course.json note); `content:import` loads 85 lessons / 244 practices (same counts, forms changed not counts); `CourseImporterTest` 10/10.
**«سناریو، User Story و Use Case» done 2026-09-22 (9 lessons, 12 explanation/scenario practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 85/244 unchanged; `CourseImporterTest` 10/10.
**«استخراج نیازمندی‌ها (Elicitation)» done 2026-09-22 (12 lessons, 22 explanation/scenario practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 85/244 unchanged; `CourseImporterTest` 10/10.
**«نوشتن و فرایند نیازمندی‌ها» done 2026-09-22 (6 lessons, 8 explanation/scenario practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 85/244 unchanged; `CourseImporterTest` 10/10.
**«نیازمندی‌های نرم‌افزار» done 2026-09-22 (10 lessons, 20 explanation/scenario practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 85/244 unchanged; `CourseImporterTest` 10/10.
**«مستندسازی و مدل‌سازی نیازمندی‌ها» done 2026-09-22 (15 lessons, 26 explanation/scenario practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 85/244 unchanged; `CourseImporterTest` 10/10.
**«مذاکره و مدیریت تعارض» done 2026-09-22 (8 lessons, 13 explanation/scenario practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 85/244 unchanged; `CourseImporterTest` 10/10.
**«مدیریت، ردیابی و جمع‌بندی» done 2026-09-22 (7 lessons, 10 explanation/scenario practices rewritten to true short_answer) — course fully migrated.** `validate.py` clean; `content:import` loads 85/244 unchanged; `CourseImporterTest` 10/10. Final form tally for `requirements-engineering`: 154 short_answer, 80 mcq, 10 coding, **0 explanation/scenario** — every one of the original 155 essay-style practices (73 explanation + 42 scenario + 40 overlong short_answer) is now a true short_answer.
✅ C-05 for `requirements-engineering` is done. Other courses (`complete-python-mastery`, `python-deep-dive-1`, `software-architecture-complete-guide`, etc.) still have `explanation`/`scenario` practices — migrating them is a separate future session, not started here.

**`python-deep-dive-1` migration started 2026-09-22.** 258 practices across 106 lessons/11 sections: 94 mcq + 80 coding stay untouched (coding's length is code, not prose — fine per AUTHORING.md). All 43 `explanation` practices get rewritten. For `short_answer`, checked each candidate individually rather than by a length threshold: most "long" ones are legitimate compact multi-item predictions (e.g. "value of these 5 expressions", "valid or not for these 5 names") which the owner explicitly said is fine — only the ones whose prompt asks for actual prose reasoning ("در ۲/۳ جمله بگو چرا...") get rewritten. That's 6 short_answer practices, for **49 total** across 9 sections (نمره‌گذاری دقیق‌تر از تخمین اولیه‌ی ۷۴ مورد). Per-section: متغیرها و حافظه (2), انواع عددی (3), پارامترهای تابع (2), توابع درجه‌یک (9), دامنه‌ها/closureها/decoratorها (9), tuple/named tuple (6), ماژول‌ها/پکیج‌ها/namespaceها (12), به‌روزرسانی‌های پایتون (2), مطالب تکمیلی (4) — «شروع» and «مرور سریع» need none.
**«متغیرها و حافظه»، «انواع عددی»، «پارامترهای تابع» done 2026-09-22 (4 practices rewritten; 2 more short_answer candidates found compact/acceptable on inspection and left unchanged — `argument-vs-parameter/same-object`, `nonlocal-scopes/trace-layers`, `decimals-constructors-and-contexts/prec-vs-ctor` are trace-prediction-plus-one-sentence, matching the owner's "multiple short answers OK" allowance, not real essays).** `validate.py` clean; `content:import` loads 106/258.
**«توابع درجه‌یک» done 2026-09-22 (9 lessons, 9 explanation practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 106/258.
**«دامنه‌ها، closureها و decoratorها» done 2026-09-22 (9 lessons, 8 explanation practices rewritten to true short_answer; `nonlocal-scopes/trace-layers` left unchanged, compact trace question).** `validate.py` clean; `content:import` loads 106/258.
**«tuple به‌عنوان ساختار داده و named tuple» done 2026-09-22 (6 lessons, 6 explanation practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 106/258.
**«ماژول‌ها، پکیج‌ها و namespaceها» done 2026-09-22 (12 lessons, 12 explanation practices rewritten to true short_answer).** `validate.py` clean; `content:import` loads 106/258.
➡ NEXT UP: «به‌روزرسانی‌های پایتون» (2 practices) and «مطالب تکمیلی» (4 practices) — last two sections.

## M10 — UI/UX pass (found by screenshotting the real app, not just reading classes)

### [x] S-36 Course catalog cards feel sparse on wide screens, no per-course visual identity
`courses/index.blade.php`'s cards are small text-only blocks with a lot of unused space around them on desktop, and nothing (color, icon, image) distinguishes one course from another at a glance. Gets worse as more courses are added. Fixed in DECISIONS §35.

### [x] S-37 Empty states (friends with one user, weak-spots/zero) feel abandoned, no next action
A lone card floating on a large dark page, with only a passive sentence. «دوستان» specifically should probably prompt inviting someone (the app already has `REGISTRATION_CODE`) rather than just showing the one existing user. Fixed in DECISIONS §36.

### [x] S-38 Home's streak/progress bar reads as an afterthought
Meant to be the motivational centerpiece (DECISIONS §21) but is currently a thin, easy-to-miss line above the main card. Deserves more visual weight. Fixed in DECISIONS §32.

### [x] S-39 "فقط یه مرور سریع" button placement feels disconnected
Floats alone above the primary "ادامه‌ی یادگیری" card instead of reading as part of the same flow. Fixed in DECISIONS §32.

### [-] S-40 Week view: day order flips between the two grid rows — false positive, not a real bug
Originally flagged from eyeballing a screenshot. Re-checked with actual measured `getBoundingClientRect()` coordinates for each day card: row two reads جمعه(27)→شنبه(28)→یکشنبه(29)→دوشنبه(30) right-to-left, same ascending order as row one — correct. The earlier read of the screenshot was simply wrong. No code change made.

### [x] S-41 A course's lesson table is one long flat list — no collapsing, no jump-to-section
With 100+ lessons (both real courses are already this size), scrolling the course page is a very long, undifferentiated scroll. Section headers exist in the table already but don't collapse or anchor-link. Judged the most valuable of this batch — affects daily use directly, not just first impressions. Fixed in DECISIONS §33.

### [x] S-42 Badge/pill overload
Priority, level, practice form, review/learn/practice — each with its own color, stacking up next to each other, worst on mobile. Could use a more restrained, consistent color system. Fixed in DECISIONS §34.

### [ ] S-43 (content, not UI) Source videos carry a third-party watermark
Not fixable from the app side — the video files themselves (from the GIT.IR download) have a burned-in logo. Noted for awareness, not actionable here.

## M11 — Accessibility

### [x] S-44 Mobile sidebar close button has no accessible name
`lessons/show.blade.php`'s `×` button closing the mobile curriculum drawer has no `title`/`aria-label`/text — a screen reader announces only "button".

### [x] S-45 Icon-only controls use `title`, not `aria-label`
Every other icon-only button/link (search, theme, hamburger, edit, skip, session-exit) has a `title` attribute, which does work as a fallback accessible name but is weaker than `aria-label` (inconsistent screen-reader support, tooltip-only visible hint). Switch them over.

### [x] S-46 Search input has no label
`search/index.blade.php`'s `<input name="q">` relies on `placeholder` alone, which isn't a reliable accessible name (disappears on input, not consistently exposed to assistive tech). Needs a real `<label>` (visually hidden is fine).

### [x] S-47 Dropdown menus (`x-dropdown`) aren't keyboard-dismissible and don't announce state
Closes on an outside click but not on Escape; the trigger button has no `aria-expanded`/`aria-haspopup` telling assistive tech it opens a menu.

### [x] S-48 `text-faint` fails WCAG AA contrast
Measured: 2.43:1 in light mode, 2.74-3.20:1 in dark mode (against `bg`/`surface`/`surface2`) — both well under the 4.5:1 minimum for normal text. Used for practice durations, hints, timestamps and more, so this is a real readability issue, not just a technicality.

### [x] S-49 Progress bars (`x-level-bar`) carry zero accessible information
Purely decorative `<span style="width:N%">` segments — no `role="progressbar"`, `aria-valuenow`, or any text equivalent. Used on Home's course cards, the course page, and the lesson page's section-progress bar; a screen reader announces nothing about progress at all in any of these spots. Found on a second, more thorough pass (screenshots of every remaining page + a CDP console-error sweep across all main routes, which came back clean — no JS errors anywhere).

## M12 — Real-use fixes (owner's own daily use of the platform, 2026-09-16)

### [x] S-50 Persian caption switching silently reverted — root-caused, not guessed (DECISIONS §43)
`storage: { enabled: false }` on the Plyr config broke `captions.setup()`'s language persistence, so every click on «زیرنویس فارسی» got reverted to English on the very next tick. Confirmed live via CDP before and after the fix (caption overlay text, not just the menu checkmark).

### [x] S-51 Enrolling without a schedule felt like nothing happened
Both Home's «دوره‌های من» card and the course page only had a passive, easy-to-miss line when `daily_time_minutes` was unset; both now carry a real CTA to `enrollments.edit`.

### [x] S-52 «انجام دادم»: a sticky, learner-declared completion signal, separate from mastery level (DECISIONS §43)
New button on the lesson page, gated on every practice being answered correctly at least once. Fills the sidebar circle / course-page checkmark / progress counter permanently, independent of any later dip in numeric mastery from a failed review — the mastery badge itself stays honest and untouched. No schema change (`evidence.source = 'lesson_done'` on the existing `attempts` table).

### [x] S-53 `Planner::today()` only materialized once per enrollment per day
`materialize()`/`candidatesFor()` were already safely re-callable; a stale `! $existing->contains(...)` guard prevented calling them again the same day even when a lesson had just become newly eligible. Removed — the plan's own «امروز» list now tops up same-day instead of waiting for tomorrow. Not the primary answer to "move at my own pace" (that's free lesson-page navigation, already unlocked, plus S-52) but a real bug worth fixing alongside it.

### [x] S-54 «دوستان» is now an admin-only, all-time progress dashboard (DECISIONS.md §44)
First `is_admin` flag on the install (migration sets it for the owner's own account). `FriendsController` 403s non-admins; both nav menus hide the link entirely for them. Per-user numbers switched from "this week's practice count" to three all-time totals — minutes spent, distinct lessons done (mastery-or-mark-done, reusing S-52's OR logic), distinct practices ever solved.

### [x] S-55 Production 500 on lesson pages (pending migration) + both nav dropdowns opened off-screen (DECISIONS.md §45)
The `is_admin` migration from S-54 hadn't been run on production yet — `/_ops/migrate` fixed it, `/_ops/optimize` refreshed caches too, owner asked to confirm. Separately, and unrelated to any recent change: the account-menu and mobile-hamburger dropdowns both used `align="left"`, which in this RTL app anchors rightward and expands *off the left edge* of the viewport — measured at `left: -128px` (1400px viewport) and `left: -10px` (390px viewport) via CDP before the fix. Both switched to `align="right"` (anchors leftward, expands into the page); re-measured fully on-screen after.

### [x] S-56 Every `<x-icon>` size override lost to the component's own default (DECISIONS.md §46)
Owner flagged the sidebar checkmark as oversized and the nav search icon as off-center, screenshot attached. Root cause, confirmed via `getComputedStyle`: `icon.blade.php`'s `$attributes->merge()` let the component's own `w-[18px] h-[18px]` default beat every caller's real size (Tailwind's arbitrary-value utility wins the cascade over fixed-scale ones) — every icon in the app was silently stuck at 18px regardless of what its call site asked for. Fixed with `$attributes->class([... => ! $attributes->has('class')])` so the default only applies when nothing was passed. Separately, the search icon's `hidden sm:flex` override dropped `.iconbtn`'s grid-based horizontal centering at the `sm` breakpoint; switched to `sm:grid`. Re-measured both after the fix: sizes match their call sites exactly, centers align.

### [x] S-57 Auto-deploy hook was silently failing — made failures visible (DECISIONS.md §48)
Second time a deploy's automatic `migrate`/`content:import` step turned out to have silently never run (first was S-55's pending migration; this time it was a whole course's content missing from production's DB despite the files being there). `.cpanel.yml`'s `curl ... || true` was swallowing any failure with no trace anywhere. Dropped `|| true`, added `-f` to curl, and made `deploy.php` append every invocation's output to `storage/logs/deploy-hook.log` regardless of outcome — readable via the new `/_ops/deploy-log`. Doesn't prove the underlying loopback-curl trick is now reliable (unverifiable without a live deploy cycle), but the next failure, if any, will be visible instead of silent. Unblocked production immediately via manual `/_ops/migrate` + `/_ops/import`.

### [x] S-58 Persian text scrambled whenever a sentence started with an English word (DECISIONS.md §50)
Owner-reported list of 5 pre-existing bugs found using the platform for real, fixed before starting C-03's authoring. First of the 5: `dir="auto"` on ~37 elements holding authored Persian prose (key_points, mcq options, prompts, titles, …) let the browser's first-strong-character heuristic flip the *entire line* to LTR base direction whenever a key point happened to start with an English term (`bool زیرکلاس int است...`) — confirmed with a before/after screenshot of a real key point. Removed `dir="auto"` from all 37 (correctly inherit `dir="rtl"` from `<html>`), kept it only on the 4 genuinely user-supplied/language-uncertain spots (search box, answer textarea, last-answer display, a person's name). Also added `unicode-bidi: isolate` to the global `code, pre, .mono` CSS rule (previously only on `.prose-fa code`) so embedded English terms outside prose blocks — key_points' inline code, for one — render as a stable unit instead of reordering. Removed one existing `text-right` band-aid that treated the symptom instead of the cause.

### [x] S-59 Practice session overhaul: per-practice status, real history, a true cancel, no native confirm() (DECISIONS.md §51)
Remaining 4 of the owner's 5-bug list. (a) The lesson page's practice list now shows each practice's own latest status (solved/unsolved/in-progress/untouched) instead of only the lesson-level "انجام دادم" card ever showing anything — clicking a practice now goes through a new smart `session.open` route that resumes an open attempt or shows the last finished one instead of always silently starting a blank new attempt. (b) `session/show.blade.php` now lists every past attempt at the same practice (date, result, response), not just the current one's last response. (c) A real no-penalty cancel (`DELETE session/{attempt}`, deletes the open attempt entirely) wired to the nav's exit icon, distinct from "give up" (which still finalizes as incorrect). (d) The one native `window.confirm()` in the app (give-up) — and the new cancel — now use the existing `<x-modal>` component (same one used for account deletion) instead of unstyled browser chrome. Bundled a fifth, smaller item: practices can now carry their own `attachments` (same shape as a lesson's), rendered under the prompt. 7 new tests, full suite 120/120, every flow verified live via CDP (badges, both modals, an attempt actually getting deleted on cancel, a real two-attempt history rendering).

### [x] S-61 Manual host updates needed 3 separate browser visits (`/_ops/migrate`, `/_ops/import`, `/_ops/optimize`) (DECISIONS.md §56)
Owner asked to simplify the post-update routine for the manual (non-git) deploy path. Added `/_ops/update?token=T`, which runs all three steps in one request (with `prune` defaulting to true, since a host update is normally a full re-sync) — mirrors what `deploy.php` already does automatically on the git-based path. The three original actions still work individually. README's manual-upload section updated to lead with it; 1 new test, full suite 123/123.

### [x] S-62 Course catalog grouped into three display-only categories (DECISIONS.md §58)
Owner asked for the catalog page to be easier to scan now that it's 15 courses. Added `courses.category`/`category_order`, grouped the catalog into «مسیر رهبری فنی» (the 10 C-03 courses, build order), «اسکرام و اجایل» (the C-04 pair), «برنامه‌نویسی» (the two Python courses). Confirmed with the owner first: display-only grouping, no gating/unlocking; `advanced-scrum-master` stays single-categorized under the leadership bundle rather than appearing twice. Full suite 123/123.

### [x] S-63 Category headers made clickable + a courses menu in the nav (DECISIONS.md §58 follow-up)
S-62 added category sections but the headers were inert text and there was no faster way in than opening the full catalog. `Course::CATEGORIES` now centralizes the 3-category list (was inline in the controller). On `/courses`, each category header is a button that filters the grid to just that category client-side (Alpine `x-show`, no reload); headers stay visible (dimmed when inactive) so switching or resetting to "همه" never requires a separate control. The nav's "همه‌ی دوره‌ها" link became a "دوره‌ها" dropdown (desktop: `x-dropdown`; mobile: indented sub-links in the existing hamburger menu) listing "همه‌ی دوره‌ها" plus all three categories, each deep-linking to `/courses?category=...` which the controller validates against the known list (falls back to "all" on anything else) and passes as the Alpine filter's initial state. Full suite 123/123.

**Follow-up rounds (same day, owner screenshots):** dropdown widened (`w-56`→`w-72`) and rows given more padding (`py-2`→`py-3` on the shared `dropdown-link` component, app-wide) after labels were wrapping/cramped; a "دسته‌بندی‌ها" section label the owner hadn't asked for was removed; the confusing rotating chevron on category headers was dropped entirely (not a real accordion — content just toggles visibility, no collapse animation); added an order-number badge (`fa_num($course->category_order)`) on cards in ordered categories; the dropdown's `align="right"` was pinning it to the screen edge — added a direction-agnostic `align="center"` option to the shared dropdown component (physical `left-1/2 -translate-x-1/2`, works under both LTR/RTL) and switched the courses menu to it. Each round required `npm run build` — this repo ships compiled `public/build/` assets in git (no Node on the host), so a Blade-only edit using a new Tailwind class doesn't reach the browser until rebuilt; missed this twice before catching it.

**Card redesign (same day, owner sent a Udemy screenshot for reference):** replaced the small colored-letter avatar with a full-width cover image on top of each card (new `<x-course-cover>` component), title/description below, badges moved onto the cover itself (order-number "قدم N" top-start, "برداشته‌شده" top-end) instead of crammed into the title row. The cover is an **original generated SVG gradient** — deliberately not a real frame pulled from the course videos: the first-lesson frames show the instructors' faces and third-party (Udemy) branding baked into the footage, and reusing that as a persistent catalog thumbnail is a materially different, more public-facing use than playing the same licensed video inside the lesson player. Explained this to the owner instead of silently swapping the approach.

**Cover art v2 + category-section visibility fix (same day, owner: first version "خیلی مسخره"):** the gradient+circles+giant-faint-letter combo looked messy. Simplified to a diagonal gradient (`color-mix` for the darker stop), a subtle dot-grid texture, one contained corner glow, and a single centered icon picked per category (trophy for «مسیر رهبری فنی», refresh for «اسکرام و اجایل», text for «برنامه‌نویسی») instead of a stray oversized letter. Separately, filtering to one category previously still left the *other* categories' headers visible with just their grids hidden (only the grid had `x-show`, not the header) — moved `x-show` up to the whole `mb-7` section so an active filter now hides other categories' headers too; clicking the active header still resets to "همه".

### [ ] S-60 Videos unreachable from phone/other devices — Cloudflare Tunnel blocked, pivoted to download host + SSH fallback (DECISIONS.md §53/§59, in progress)
`MEDIA_BASE_URL=http://localhost:8765` only ever worked on the owner's own daily machine. `growwise.ir`'s full DNS was migrated to Cloudflare and a named tunnel built, but `cloudflared` can't reach Cloudflare's edge at all — port 7844 is blocked at the network/carrier level (confirmed on wifi and mobile data alike; `ngrok` hit the same wall). A VPN-proxied workaround was proven to work but the owner declined it as a standing dependency. **App side done:** video/caption `<source>`/`<track>` *and* attachment download links now try an optional download host first (`MEDIA_DOWNLOAD_BASE_URL`) with the local machine as an automatic browser-side fallback — see DECISIONS.md §59. **Infra side, in progress:** owner bought a Parspack download-hosting plan (100GB, `dl.growwise.ir` → Cloudflare-proxied for free SSL) and is uploading all ~72GB of course videos there via `lftp`; once done, `MEDIA_DOWNLOAD_BASE_URL` gets set in production and this closes out. SSH-from-Parspack + reverse-tunnel is still the noted fallback if the download host doesn't pan out. **See DECISIONS.md §53/§59 for exact status.**

### [ ] S-61 Gemini genuinely unreachable from Iran-hosted production — relay VPS pending (DECISIONS.md §60/§61)
`GeminiClient` (grading every non-mcq practice, and generating new review practices) can't reach Google directly from production — confirmed with the real API key and a real request, not a bad test. App-side fix is done: `GeminiClient` optionally routes through a relay (`AI_PROXY_URL`) via `App\Services\Ai\Concerns\UsesOutboundProxy`, ported from the sibling project EnglishOS's identical, already-proven setup. A laptop+`localhost.run`-tunnel version of the relay was tried and abandoned — it collided with and briefly broke EnglishOS's own live tunnel (same source IP). **Current plan: a small dedicated VPS outside Iran**, same as EnglishOS already runs — `tools/vps-relay-setup.sh` + `tools/vps-relay-README.fa.md` are ready, just need the owner to provision the VPS and run the script. `/_ops/ai-check` (new) round-trips a real Gemini call so this is easy to confirm once wired up. **See DECISIONS.md §60/§61 for exact status.**
