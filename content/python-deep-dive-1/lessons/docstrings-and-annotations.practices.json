[
  {
    "key": "which-is-docstring",
    "title": "کدام docstring است؟",
    "form": "mcq",
    "difficulty": "intro",
    "estimated_minutes": 2,
    "prompt": "```python\ndef f(x):\n    # computes the square\n    'squares x'\n    'not this one'\n    return x ** 2\n```\n`f.__doc__` چیست؟",
    "options": ["'# computes the square'", "'squares x'", "'not this one'", "None — چون اولین خط comment است"],
    "correct_option": 1,
    "expected_outcome": "comment کد نیست و حذف می‌شود؛ اولین *خط کد* بدنه رشته‌ی `'squares x'` است، پس docstring همان است. رشته‌ی دوم فقط یک رشته‌ی بی‌اثر است.",
    "hints": ["comment اصلاً کامپایل نمی‌شود.", "اولین خط کد بدنه کدام است؟"],
    "rubric": "Correct only if option 1 is selected."
  },
  {
    "key": "annotations-dict",
    "title": "`__annotations__` را بخوان",
    "form": "short_answer",
    "difficulty": "core",
    "estimated_minutes": 3,
    "prompt": "```python\ndef area(w: float, h: float = 1.0, *, unit: 'cm or m' = 'cm') -> float:\n    return w * h\n```\n(۱) `area.__annotations__` دقیقاً چه dict‌ای است؟ (۲) اگر `area('3', 2)` صدا بزنیم چه می‌شود و چرا؟",
    "expected_outcome": "(۱) `{'w': float, 'h': float, 'unit': 'cm or m', 'return': float}` — کلیدها نام پارامترها به‌علاوه‌ی `'return'`؛ مقدارها همان عبارت‌های annotation (اینجا خود کلاس `float` و یک رشته). (۲) بدون خطا `'33'` برمی‌گرداند (`'3' * 2`)؛ annotation فقط متادیتا است و پایتون نوع را چک نمی‌کند.",
    "hints": ["کلید مقدار برگشتی چه نامی دارد؟", "annotation اجرای کد را تغییر نمی‌دهد."],
    "rubric": "Correct: dict with w, h, unit, return keys and right values (float class, string) AND says the call runs and returns '33' because annotations are not enforced. Partial: dict right but claims a TypeError, or misses the return key. Incorrect: otherwise."
  },
  {
    "key": "eval-once",
    "title": "annotation کِی ارزیابی می‌شود؟",
    "form": "short_answer",
    "difficulty": "stretch",
    "estimated_minutes": 2,
    "prompt": "annotation یه پارامتر با `'must be below ' + str(limit)` نوشته شده، وقتی limit بعداً عوض می‌شه. این annotation کِی ارزیابی می‌شه — موقع def یا هر فراخوانی؟",
    "expected_outcome": "موقع def (یک‌بار، نه هر فراخوانی)",
    "hints": ["def خودش یک بار اجرا می‌شود.", "درس «مراقب مقدارهای پیش‌فرض باش» را به یاد بیاور."],
    "rubric": "Correct if response says annotations are evaluated once at def time, like default values."
  }
]
