[
  {
    "key": "name-value",
    "title": "__name__ چیست؟",
    "form": "mcq",
    "difficulty": "intro",
    "estimated_minutes": 2,
    "prompt": "`app.py` را با `python app.py` اجرا می‌کنیم و app.py داخلش `import tools` دارد که tools.py هم `import helpers` دارد. `__name__` در هر سه فایل چیست؟",
    "options": ["'app', 'tools', 'helpers'", "'__main__', 'tools', 'helpers'", "'__main__', '__main__', '__main__'", "'app.py', 'tools.py', 'helpers.py'"],
    "correct_option": 1,
    "expected_outcome": "فقط نقطه‌ی ورود `__main__` می‌شود؛ ماژول‌های import‌شده نام خودشان (بدون .py) را دارند، مستقل از اینکه چند سطح پایین‌تر import شده‌اند.",
    "hints": ["فقط یک فایل مستقیم اجرا شده.", "نام ماژول پسوند ندارد."],
    "rubric": "Correct only if option 1 is selected."
  },
  {
    "key": "dual-use-module",
    "title": "ماژول دومنظوره",
    "form": "coding",
    "difficulty": "core",
    "estimated_minutes": 6,
    "prompt": "ماژولی به نام `wordcount.py` بنویس با تابع `count_words(text) -> dict` (تعداد هر کلمه) که (۱) با `import wordcount` بدون هیچ اثر جانبی قابل استفاده باشد و (۲) با `python wordcount.py FILE [-n N]` پرتکرارترین N کلمه‌ی یک فایل را چاپ کند (پیش‌فرض N=5) با `argparse`. توضیح بده چرا بلوک خط فرمان هنگام import اجرا نمی‌شود.",
    "expected_outcome": "```python\n\"\"\"Counts words in a text file.\"\"\"\nimport argparse\nfrom collections import Counter\n\ndef count_words(text):\n    return dict(Counter(text.split()))\n\nif __name__ == '__main__':\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument('file')\n    parser.add_argument('-n', type=int, default=5)\n    args = parser.parse_args()\n    with open(args.file) as f:\n        counts = count_words(f.read())\n    for word, n in sorted(counts.items(), key=lambda kv: kv[1], reverse=True)[:args.n]:\n        print(word, n)\n```\nهنگام import، `__name__ == 'wordcount'` است نه `'__main__'`، پس بلوک اجرا نمی‌شود.",
    "hints": ["همه‌ی کد خط فرمان زیر if __name__ == '__main__'.", "argparse: یک positional و یک -n."],
    "rubric": "Correct: importable function, argparse CLI under the __main__ guard, top-N output, and the __name__ explanation. Partial: CLI works but parsing/printing at module level (runs on import). Incorrect: no guard."
  },
  {
    "key": "main-py",
    "title": "python somedir/ چه فایلی رو اجرا می‌کنه؟",
    "form": "short_answer",
    "difficulty": "core",
    "estimated_minutes": 2,
    "prompt": "python somedir/ چه فایلی رو به‌عنوان نقطه‌ی ورود اجرا می‌کنه؟",
    "expected_outcome": "__main__.py",
    "hints": ["نام فایل ورودی پوشه چیست؟", "اگه نباشه، خطا می‌ده."],
    "rubric": "Correct only if response names __main__.py."
  }
]
