[
  {
    "key": "zip-shortest",
    "title": "zip کجا می‌ایستد؟",
    "form": "mcq",
    "difficulty": "intro",
    "estimated_minutes": 2,
    "prompt": "```python\nlist(map(lambda x, y: x * y, [1, 2, 3, 4], (10, 20)))\n```\nنتیجه چیست؟",
    "options": ["[10, 40, 30, 40]", "[10, 40]", "TypeError — طول‌ها برابر نیست", "[10, 40, None, None]"],
    "correct_option": 1,
    "expected_outcome": "map (مثل zip) با اتمام کوتاه‌ترین iterable متوقف می‌شود؛ tuple دو عنصر دارد، پس فقط دو حاصل‌ضرب.",
    "hints": ["map چند iterable را به‌موازات مصرف می‌کند.", "کدام iterable زودتر تمام می‌شود؟"],
    "rubric": "Correct only if option 1 is selected."
  },
  {
    "key": "rewrite-comprehension",
    "title": "بازنویسی به comprehension",
    "form": "coding",
    "difficulty": "core",
    "estimated_minutes": 5,
    "prompt": "این عبارت را با یک list comprehension بازنویسی کن و خروجی را بنویس:\n```python\nnames = ['ali', 'bob', 'cara', 'dan']\nages = [30, 17, 25, 15]\nlist(filter(lambda t: t[1] >= 18, map(lambda n, a: (n.title(), a), names, ages)))\n```",
    "expected_outcome": "```python\n[(n.title(), a) for n, a in zip(names, ages) if a >= 18]\n# [('Ali', 30), ('Cara', 25)]\n```\nزوج‌سازی دو لیست با zip + unpacking، فیلتر با if.",
    "hints": ["برای پیمایش دو لیست به‌موازات، zip و unpacking در for.", "شرط filter به if انتهای comprehension می‌رود."],
    "rubric": "Correct: comprehension with zip(names, ages), tuple unpacking, .title(), if a >= 18, and the right output. Partial: right structure but forgets title() or the condition, or wrong output. Incorrect: still uses map/filter or iterates by index without need."
  },
  {
    "key": "exhausted-iterator",
    "title": "چرا بار دوم خالی است؟",
    "form": "short_answer",
    "difficulty": "core",
    "estimated_minutes": 2,
    "prompt": "`pairs = zip(...)` رو دوبار `list(pairs)` می‌کنیم؛ بار دوم خالیه. چرا؟",
    "expected_outcome": "zip یک iterator یک‌بارمصرفه (بعد از پیمایش کامل، exhausted می‌شه)",
    "hints": ["iterator بعد از پیمایش کامل، خالی است.", "list comprehension همه را همان لحظه محاسبه می‌کند."],
    "rubric": "Correct if response says zip returns a lazy, one-shot iterator that's exhausted after one full pass."
  }
]
